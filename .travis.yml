sudo: false
language: ruby
cache: bundler
rvm:
- 2.5.1

install:
  # Install Jekyll and dependencies
  - bundle install

script: bundle exec rake site:deploy --quiet #--trace

# Generate your secure token with the travis gem:
# get Github token from your Travis CI profile page
gem install travis
# GH_TOKEN from https://github.com/settings/tokens
travis encrypt 'GIT_NAME="Openanttony" GIT_EMAIL="antony@leons.dev" GH_TOKEN=f0c4e0bc84cee5bc31663c162d178547cc5cba39'
env:
  global:
    secure: "BcpzyVcLZ23HBHElaK8S5w"
branches:
  only:
    - master
